import { styled } from "@mui/material";

export const Image = styled('div')({
    position: 'absolute',
    top: '0',
    width: '100%',
    height: '17vh'
})

export const DivSubTitle = styled('div')({
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    'img': {
        width: '40%',
        minWidth: '180px'
    }
}) 